[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

testpaths = src/tests

# PYTHONPATH를 src로 설정합니다.
pythonpath = src

markers =
    unit: Unit tests
    integration: Integration tests
    ui: UI component tests

addopts = -v --tb=short